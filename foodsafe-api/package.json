{
  "name": "foodsafe-api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server/index.ts",
    "dev:frontend": "cd frontend && bun run dev",
    "ingest": "bun run src/ingestion/worker.ts",
    "db:init": "bun run src/server/db/init.ts",
    "db:migrate:pg": "bun run src/server/db/migrate-to-pg.ts"
  },
  "dependencies": {
    "gap-common": "file:../common",
    "stripe": "^17.4.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
