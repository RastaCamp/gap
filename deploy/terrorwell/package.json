{
  "name": "terrorwell-pages",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=terrorwell --branch=main"
  },
  "devDependencies": {
    "wrangler": "^4.93.0"
  }
}
